/**
 * Feature flags — toggle unreleased features without touching production.
 * Flip CLOUD_BACKUP to true when Firebase + RevenueCat are configured.
 */
export const FEATURES = {
    CLOUD_BACKUP: false,
};
